export const MAX_BOARD_SIZE = 10;
export const MIN_BOARD_SIZE = 5;

export const ROUTES = {
  LOGIN: '/',
  GAME: '/game',
  HISTORY: '/history',
};

export const GAME_STATUS = {
  PLAYING: 'playing',
  WIN: 'win',
  DRAW: 'draw',
} as const;
